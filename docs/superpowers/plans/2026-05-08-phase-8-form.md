# Phase 8 — Plano de Implementação do Form

> **Para agentes executores:** SUB-SKILL OBRIGATÓRIO: usar superpowers:subagent-driven-development (recomendado) ou superpowers:executing-plans para implementar tarefa por tarefa. Steps usam sintaxe `- [ ]` para tracking.

**Objetivo:** Conectar o `ReserveForm` ao Formspree (backup de leads) + Cloudflare Turnstile (anti-bot) + deeplink WhatsApp (canal real de conversão), com cobertura TDD para utilitários puros (montador de URL + máscara de telefone + extrator de FormData).

**Arquitetura:** O form tem 3 camadas de defesa (honeypot, widget Turnstile, validação HTML5 nativa). O submit dispara POST pro Formspree (fire-and-forget, sem await) e **sempre** redireciona o usuário pra `wa.me/<num>?text=<resumo>`. WhatsApp é o canal real; Formspree é só rastro de papel pra backup. O `whatsappNumber` flui de `src/content/site.yml` → atributo `data-whatsapp-number` no `<form>` → `form.dataset.whatsappNumber` no handler. Os utilitários puros (`whatsapp.ts`) ficam 100% testados com Vitest; o handler do DOM (`reserve-form.ts`) é validado por smoke test manual no browser.

**Stack:** Astro 5 + TypeScript (strict) + Vitest (novo) + Formspree (free tier, REST POST) + Cloudflare Turnstile (Managed challenge, widget) + FormData/HTML5 validation nativo. Fonte de verdade: `docs/patterns/forms.md`.

**Resolução de conflito:** A Phase 8 em `docs/superpowers/plans/2026-05-04-cinnamon-drinks-implementation.md` (linhas 2229-2558) usava `await` no Formspree + mensagem WhatsApp com emojis + sem checkbox LGPD. O pattern em `docs/patterns/forms.md` é a fonte de verdade (mais recente, alinhado com §4.4 + §4.7 do spec) — POST fire-and-forget, mensagem WhatsApp em formato `label: valor`, LGPD obrigatório e desmarcado por padrão. **Este plano segue o pattern.**

---

## Estrutura de Arquivos

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `package.json` | Modificar | adicionar devDep `vitest` + scripts `test` / `test:watch` |
| `vitest.config.ts` | Criar | config do Vitest (env Node, sem DOM) |
| `src/scripts/whatsapp.ts` | Criar | utils puros: interface `Lead`, `buildWhatsappUrl`, `maskPhone`, `getLeadFromFormData` |
| `src/scripts/whatsapp.test.ts` | Criar | suite Vitest dos utils puros (12 testes) |
| `src/scripts/reserve-form.ts` | Criar | handler DOM — wire submit, valida, dispara POST, redireciona WhatsApp, mascara input de telefone |
| `src/components/ReserveForm.astro` | Modificar | adicionar checkbox LGPD, atributo `data-whatsapp-number`, action real do Formspree, import `<script>` |
| `src/layouts/Base.astro` | Modificar | adicionar `<script>` do widget Turnstile no `<head>` |
| `src/content/form.yml` | Modificar | adicionar campo `lgpdLabel` para o texto do checkbox |
| `src/content.config.ts` | Modificar | estender schema `form` com `lgpdLabel: z.string()` |
| `src/pages/privacidade.astro` | Criar | página mínima de política de privacidade LGPD (linkada do checkbox) |
| `src/content/site.yml` | Verificar | confirmar que `whatsappNumber` existe e bate com `^\d{12,13}$` |

**Boundaries:** `whatsapp.ts` é 100% puro (sem DOM, sem fetch, sem `window`) — totalmente testável em unit. `reserve-form.ts` é o único arquivo autorizado a tocar `window`/`document`/`fetch` — concentra side-effects num lugar só.

---

## Tarefa 1: Setup do Vitest

**Arquivos:**
- Modificar: `package.json`
- Criar: `vitest.config.ts`

- [ ] **Passo 1: Instalar Vitest como devDep**

```bash
pnpm add -D vitest
```

Esperado: `package.json` ganha `"vitest": "^X.Y.Z"` (qualquer 1.x ou 2.x) em `devDependencies`; `pnpm-lock.yaml` atualiza.

- [ ] **Passo 2: Adicionar scripts `test` e `test:watch` no package.json**

No `package.json`, o bloco `scripts` atualmente termina com `"prepare": "simple-git-hooks"`. Adicionar duas entradas novas (não remover as existentes):

```json
"test": "vitest run",
"test:watch": "vitest"
```

Bloco final de `scripts` deve conter (a ordem não importa):

```json
"scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "check": "biome check .",
  "lint": "biome lint .",
  "format": "biome format --write .",
  "ts:check": "astro check",
  "cms:proxy": "pnpm dlx @sveltia/cms-proxy-server",
  "test": "vitest run",
  "test:watch": "vitest",
  "prepare": "simple-git-hooks"
}
```

- [ ] **Passo 3: Criar `vitest.config.ts`**

Criar na raiz do projeto:

```ts
// vitest.config.ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

**Por que env `node`:** todos os testes deste plano são puros (sem DOM). `FormData` é global no Node 18+ — não precisa de jsdom/happy-dom.

- [ ] **Passo 4: Verificar que o Vitest carrega (sanity check)**

```bash
pnpm test
```

Esperado: sai com `No test files found, exiting with code 1` OU `0 passed` — qualquer um serve; só precisa que o Vitest carregue sem crashar.

- [ ] **Passo 5: Commit**

```bash
git add package.json pnpm-lock.yaml vitest.config.ts
git commit -m "chore(test): adiciona vitest para suite de utils do form"
```

---

## Tarefa 2: TDD do `buildWhatsappUrl` (util puro)

**Arquivos:**
- Criar: `src/scripts/whatsapp.test.ts`
- Criar: `src/scripts/whatsapp.ts`

- [ ] **Passo 1: Escrever testes (failing) para `buildWhatsappUrl`**

Criar `src/scripts/whatsapp.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { buildWhatsappUrl, type Lead } from './whatsapp';

const fullLead: Lead = {
  nome: 'Maria Silva',
  telefone: '(35) 99999-8888',
  email: 'maria@example.com',
  evento: 'Casamento',
  data: '2026-12-20',
  convidados: '120',
  local: 'Lavras/MG',
  duracao: '6 horas',
  mensagem: 'Quero degustação prévia.',
};

describe('buildWhatsappUrl', () => {
  it('returns wa.me link with digits-only phone', () => {
    const url = buildWhatsappUrl({ phone: '5535998224771', lead: fullLead });
    expect(url.startsWith('https://wa.me/5535998224771?text=')).toBe(true);
  });

  it('strips non-digits from the phone before building the URL', () => {
    const url = buildWhatsappUrl({ phone: '+55 (35) 99822-4771', lead: fullLead });
    expect(url.startsWith('https://wa.me/5535998224771?text=')).toBe(true);
  });

  it('encodes the message body (lead fields appear when decoded)', () => {
    const url = buildWhatsappUrl({ phone: '5535998224771', lead: fullLead });
    const decoded = decodeURIComponent(url.split('text=')[1]);
    expect(decoded).toContain('Maria Silva');
    expect(decoded).toContain('Casamento');
    expect(decoded).toContain('120');
    expect(decoded).toContain('Lavras/MG');
    expect(decoded).toContain('Quero degustação prévia.');
  });

  it('omits empty optional fields (email, local, duracao, mensagem) from the message', () => {
    const minimal: Lead = {
      nome: 'João',
      telefone: '31988887777',
      email: '',
      evento: 'Aniversário',
      data: '2026-08-01',
      convidados: '40',
      local: '',
      duracao: '',
      mensagem: '',
    };
    const url = buildWhatsappUrl({ phone: '5535998224771', lead: minimal });
    const decoded = decodeURIComponent(url.split('text=')[1]);
    expect(decoded).not.toContain('Email:');
    expect(decoded).not.toContain('Local:');
    expect(decoded).not.toContain('Duração:');
    expect(decoded).not.toContain('Mensagem:');
    expect(decoded).toContain('João');
    expect(decoded).toContain('Aniversário');
  });
});
```

- [ ] **Passo 2: Rodar — confirmar que falha**

```bash
pnpm test
```

Esperado: FAIL com `Cannot find module './whatsapp'` (ou erro de resolução equivalente).

- [ ] **Passo 3: Implementar `whatsapp.ts` (mínimo — só `Lead` + `buildWhatsappUrl`)**

Criar `src/scripts/whatsapp.ts`:

```ts
export interface Lead {
  nome: string;
  telefone: string;
  email: string;
  evento: string;
  data: string;
  convidados: string;
  local: string;
  duracao: string;
  mensagem: string;
}

export function buildWhatsappUrl(args: { phone: string; lead: Lead }): string {
  const { phone, lead } = args;
  const onlyDigits = phone.replace(/\D/g, '');
  const lines = [
    'Olá! Quero um orçamento Cinnamon Drinks.',
    '',
    `Nome: ${lead.nome}`,
    `WhatsApp: ${lead.telefone}`,
    lead.email && `Email: ${lead.email}`,
    `Evento: ${lead.evento}`,
    `Data: ${lead.data}`,
    `Convidados: ${lead.convidados}`,
    lead.local && `Local: ${lead.local}`,
    lead.duracao && `Duração: ${lead.duracao}`,
    lead.mensagem && `\nMensagem:\n${lead.mensagem}`,
  ].filter(Boolean);
  return `https://wa.me/${onlyDigits}?text=${encodeURIComponent(lines.join('\n'))}`;
}
```

- [ ] **Passo 4: Rodar — confirmar que 4 passam**

```bash
pnpm test
```

Esperado: `Test Files 1 passed`, `Tests 4 passed`.

- [ ] **Passo 5: Commit**

```bash
git add src/scripts/whatsapp.ts src/scripts/whatsapp.test.ts
git commit -m "feat(form): adiciona buildWhatsappUrl com cobertura TDD"
```

---

## Tarefa 3: TDD do `maskPhone` (util puro)

**Arquivos:**
- Modificar: `src/scripts/whatsapp.test.ts`
- Modificar: `src/scripts/whatsapp.ts`

- [ ] **Passo 1: Adicionar testes (failing) para `maskPhone` no fim do arquivo**

Acrescentar em `src/scripts/whatsapp.test.ts` (após o `describe` existente, antes do EOF):

```ts
import { maskPhone } from './whatsapp';

describe('maskPhone', () => {
  it('formats 11 digits as (XX) XXXXX-XXXX', () => {
    expect(maskPhone('35999998888')).toBe('(35) 99999-8888');
  });

  it('formats 10 digits as (XX) XXXX-XXXX', () => {
    expect(maskPhone('3533334444')).toBe('(35) 3333-4444');
  });

  it('formats partial input progressively', () => {
    expect(maskPhone('35')).toBe('35');
    expect(maskPhone('359')).toBe('(35) 9');
    expect(maskPhone('359999')).toBe('(35) 9999');
    expect(maskPhone('3599998')).toBe('(35) 9999-8');
  });

  it('caps at 11 digits (extras ignored)', () => {
    expect(maskPhone('35999998888999')).toBe('(35) 99999-8888');
  });

  it('strips non-digits from input before formatting', () => {
    expect(maskPhone('(35) 99999-8888')).toBe('(35) 99999-8888');
    expect(maskPhone('+55 35 9 9999 8888')).toBe('(55) 35999-9888');
  });
});
```

- [ ] **Passo 2: Rodar — confirmar que falha**

```bash
pnpm test
```

Esperado: FAIL com `maskPhone is not a function` ou erro de import.

- [ ] **Passo 3: Adicionar `maskPhone` em `whatsapp.ts`**

Acrescentar em `src/scripts/whatsapp.ts` (depois de `buildWhatsappUrl`):

```ts
export function maskPhone(raw: string): string {
  const d = raw.replace(/\D/g, '').slice(0, 11);
  if (d.length <= 2) return d;
  if (d.length <= 6) return `(${d.slice(0, 2)}) ${d.slice(2)}`;
  if (d.length <= 10) return `(${d.slice(0, 2)}) ${d.slice(2, 6)}-${d.slice(6)}`;
  return `(${d.slice(0, 2)}) ${d.slice(2, 7)}-${d.slice(7)}`;
}
```

- [ ] **Passo 4: Rodar — confirmar que os 9 passam**

```bash
pnpm test
```

Esperado: `Test Files 1 passed`, `Tests 9 passed` (4 da Tarefa 2 + 5 da Tarefa 3).

- [ ] **Passo 5: Commit**

```bash
git add src/scripts/whatsapp.ts src/scripts/whatsapp.test.ts
git commit -m "feat(form): adiciona maskPhone (BR) com cobertura TDD"
```

---

## Tarefa 4: TDD do `getLeadFromFormData` (util puro)

**Arquivos:**
- Modificar: `src/scripts/whatsapp.test.ts`
- Modificar: `src/scripts/whatsapp.ts`

**Por que esse util:** mantém o handler do DOM enxuto — handler cria `FormData(form)`, entrega pra essa função pura, recebe um `Lead` tipado. Testável sem jsdom.

- [ ] **Passo 1: Adicionar testes (failing) para `getLeadFromFormData`**

Acrescentar em `src/scripts/whatsapp.test.ts`:

```ts
import { getLeadFromFormData } from './whatsapp';

describe('getLeadFromFormData', () => {
  it('extracts all 9 lead fields from FormData', () => {
    const fd = new FormData();
    fd.append('nome', 'Maria');
    fd.append('telefone', '(35) 99999-8888');
    fd.append('email', 'maria@example.com');
    fd.append('evento', 'Casamento');
    fd.append('data', '2026-12-20');
    fd.append('convidados', '120');
    fd.append('local', 'Lavras/MG');
    fd.append('duracao', '6 horas');
    fd.append('mensagem', 'oi');

    const lead = getLeadFromFormData(fd);
    expect(lead).toEqual({
      nome: 'Maria',
      telefone: '(35) 99999-8888',
      email: 'maria@example.com',
      evento: 'Casamento',
      data: '2026-12-20',
      convidados: '120',
      local: 'Lavras/MG',
      duracao: '6 horas',
      mensagem: 'oi',
    });
  });

  it('coerces missing fields to empty strings', () => {
    const fd = new FormData();
    fd.append('nome', 'João');
    const lead = getLeadFromFormData(fd);
    expect(lead.nome).toBe('João');
    expect(lead.email).toBe('');
    expect(lead.local).toBe('');
    expect(lead.mensagem).toBe('');
  });

  it('ignores unknown fields like _gotcha and lgpd', () => {
    const fd = new FormData();
    fd.append('nome', 'Maria');
    fd.append('_gotcha', 'spam-bot-filled-this');
    fd.append('lgpd', 'on');
    const lead = getLeadFromFormData(fd);
    expect(lead).not.toHaveProperty('_gotcha');
    expect(lead).not.toHaveProperty('lgpd');
  });
});
```

- [ ] **Passo 2: Rodar — confirmar que falha**

```bash
pnpm test
```

Esperado: FAIL com `getLeadFromFormData is not a function`.

- [ ] **Passo 3: Adicionar `getLeadFromFormData` em `whatsapp.ts`**

Acrescentar em `src/scripts/whatsapp.ts`:

```ts
const LEAD_FIELDS = [
  'nome',
  'telefone',
  'email',
  'evento',
  'data',
  'convidados',
  'local',
  'duracao',
  'mensagem',
] as const satisfies ReadonlyArray<keyof Lead>;

export function getLeadFromFormData(fd: FormData): Lead {
  const lead = {} as Lead;
  for (const field of LEAD_FIELDS) {
    const value = fd.get(field);
    lead[field] = typeof value === 'string' ? value : '';
  }
  return lead;
}
```

- [ ] **Passo 4: Rodar — confirmar que os 12 passam**

```bash
pnpm test
```

Esperado: `Test Files 1 passed`, `Tests 12 passed`.

- [ ] **Passo 5: Commit**

```bash
git add src/scripts/whatsapp.ts src/scripts/whatsapp.test.ts
git commit -m "feat(form): adiciona getLeadFromFormData typed extractor"
```

---

## Tarefa 5: Adicionar campo LGPD no schema e no `form.yml`

**Arquivos:**
- Modificar: `src/content.config.ts:107-115`
- Modificar: `src/content/form.yml`

- [ ] **Passo 1: Estender o schema `form` em `content.config.ts`**

Em `src/content.config.ts`, localizar a collection `form` (linhas 107-115) e adicionar `lgpdLabel`:

```ts
const form = defineCollection({
  loader: file('src/content/form.yml'),
  schema: z.object({
    heading: z.string().default('Reservar minha data'),
    ctaButton: z.string().default('Reservar minha data'),
    successMessage: z.string(),
    errorMessage: z.string(),
    lgpdLabel: z.string().default('Concordo com a Política de Privacidade'),
  }),
});
```

- [ ] **Passo 2: Adicionar o campo no `form.yml`**

Em `src/content/form.yml`, adicionar no fim do bloco `form:`:

```yml
form:
  heading: Reservar minha data
  ctaButton: Reservar minha data
  successMessage: |
    Recebemos seu pedido! Em instantes você será redirecionado para o
    WhatsApp para conversarmos sobre seu evento.
  errorMessage: |
    Ops, algo deu errado. Tente novamente ou nos chame direto pelo
    WhatsApp.
  lgpdLabel: Concordo com a [Política de Privacidade](/privacidade) e em ser contatado(a) via WhatsApp/email.
```

(O link aparece como markdown no YAML — a Tarefa 7 converte de volta pra HTML no componente Astro.)

- [ ] **Passo 3: Verificar que o schema aceita o arquivo**

```bash
pnpm ts:check
```

Esperado: 0 erros. (`astro check` roda validação das content collections.)

- [ ] **Passo 4: Commit**

```bash
git add src/content.config.ts src/content/form.yml
git commit -m "feat(form): adiciona campo lgpdLabel no schema do form"
```

---

## Tarefa 6: Criar a página `/privacidade`

**Arquivos:**
- Criar: `src/pages/privacidade.astro`

**Por quê:** O checkbox LGPD linka pra `/privacidade`. 404 ali = bandeira vermelha jurídica (link pra política inexistente = consentimento inválido). Página mínima já basta pro MVP — Roger pode iterar depois via Sveltia (Phase 6).

- [ ] **Passo 1: Criar a página**

```astro
---
// src/pages/privacidade.astro
import Base from '../layouts/Base.astro';
import { getEntry } from 'astro:content';

const site = await getEntry('site', 'site');
---

<Base title="Política de Privacidade" description="Como a Cinnamon Drinks trata seus dados ao solicitar orçamento.">
  <main class="legal-page">
    <h1>Política de Privacidade</h1>
    <p><strong>Última atualização:</strong> 8 de maio de 2026</p>

    <h2>1. Quais dados coletamos</h2>
    <p>
      Ao preencher o formulário de reserva, coletamos: nome, telefone (WhatsApp),
      e-mail (opcional), tipo de evento, data desejada, número de convidados,
      local, duração estimada e mensagem livre.
    </p>

    <h2>2. Para quê usamos</h2>
    <p>
      Apenas para retornar o contato e elaborar o orçamento solicitado. Não
      vendemos, alugamos, nem compartilhamos seus dados com terceiros.
    </p>

    <h2>3. Onde ficam armazenados</h2>
    <p>
      Cópia de backup no Formspree (servidor nos EUA, política em
      <a href="https://formspree.io/legal/privacy-policy/">formspree.io/legal/privacy-policy</a>)
      e na conversa de WhatsApp com a equipe Cinnamon Drinks.
    </p>

    <h2>4. Por quanto tempo</h2>
    <p>
      Mantemos os dados pelo período necessário ao atendimento (até 12 meses
      após o último contato). Você pode solicitar exclusão a qualquer momento
      pelo e-mail abaixo.
    </p>

    <h2>5. Seus direitos (LGPD)</h2>
    <p>
      Conforme a Lei Geral de Proteção de Dados (Lei 13.709/2018), você pode
      solicitar a confirmação, acesso, correção ou eliminação dos seus dados.
    </p>

    <h2>6. Contato</h2>
    <p>
      <strong>Cinnamon Drinks</strong><br />
      E-mail: <a href={`mailto:${site!.data.email}`}>{site!.data.email}</a><br />
      WhatsApp: {site!.data.whatsappNumber}
    </p>

    <p><a href="/">← Voltar para o site</a></p>
  </main>
</Base>

<style>
  .legal-page {
    max-width: 720px;
    margin: 0 auto;
    padding: 6rem 1.5rem 4rem;
    font-family: 'Outfit', system-ui, sans-serif;
    color: #000;
    line-height: 1.6;
  }
  .legal-page h1 { font-weight: 700; margin-bottom: 0.5rem; }
  .legal-page h2 { font-weight: 700; margin-top: 2rem; margin-bottom: 0.5rem; }
  .legal-page a { color: #000; text-decoration: underline; }
</style>
```

- [ ] **Passo 2: Verificar que a página builda**

```bash
pnpm build
```

Esperado: build conclui; `dist/privacidade/index.html` existe.

- [ ] **Passo 3: Commit**

```bash
git add src/pages/privacidade.astro
git commit -m "feat(pages): adiciona página de política de privacidade (LGPD)"
```

---

## Tarefa 7: Modificar `ReserveForm.astro` — checkbox LGPD + atributo `data-whatsapp-number` + import do script

**Arquivos:**
- Modificar: `src/components/ReserveForm.astro`

- [ ] **Passo 1: Importar `site` no frontmatter + adicionar `data-whatsapp-number` no `<form>`**

Em `src/components/ReserveForm.astro`, modificar o frontmatter (linhas 1-15) para também carregar `site`:

```astro
---
// src/components/ReserveForm.astro
import { getEntry } from 'astro:content';

const form = await getEntry('form', 'form');
const site = await getEntry('site', 'site');
const data = form!.data;
const whatsappNumber = site!.data.whatsappNumber;

const minDate = new Date(Date.now() + 30 * 86400_000).toISOString().slice(0, 10);
---
```

Depois modificar a tag de abertura do `<form>` (atualmente linhas 26-34) para adicionar o data-attr:

```astro
<form
  id="cinnamon-reserve-form"
  name="cinnamon-reserve-form"
  method="POST"
  action="/.netlify/functions/-"
  class="reserve-form"
  novalidate
  data-cinnamon-form
  data-whatsapp-number={whatsappNumber}
>
```

(`action` continua placeholder por enquanto — a Tarefa 10 troca pelo URL real do Formspree.)

- [ ] **Passo 2: Adicionar checkbox LGPD antes do submit**

Em `src/components/ReserveForm.astro`, localizar o bloco do widget Turnstile (atualmente linhas 157-163) e **inserir** o checkbox LGPD **antes dele**:

```astro
<label class="reserve-lgpd">
  <input type="checkbox" name="lgpd" required />
  <span set:html={data.lgpdLabel.replace(
    /\[(.+?)\]\((.+?)\)/g,
    '<a href="$2">$1</a>'
  )} />
</label>

<!-- Turnstile widget (sitekey configurado na Tarefa 10) -->
<div
  class="cf-turnstile"
  data-sitekey="TURNSTILE_SITEKEY_PLACEHOLDER"
  data-theme="dark"
>
</div>
```

(O regex transforma o link estilo markdown do `lgpdLabel` em uma tag `<a>` — mantém o conteúdo simples no YAML.)

- [ ] **Passo 3: Adicionar `<script>` no fim do arquivo**

Depois da tag `</section>` em `src/components/ReserveForm.astro`, acrescentar:

```astro
<script>
  import '../scripts/reserve-form.ts';
</script>
```

- [ ] **Passo 4: Adicionar CSS mínimo do checkbox LGPD no mesmo arquivo**

Acrescentar um bloco `<style>` depois do `<script>`:

```astro
<style>
  .reserve-lgpd {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    margin: 1rem 0;
    font-size: 0.875rem;
    line-height: 1.4;
    color: #fff;
  }
  .reserve-lgpd input[type='checkbox'] {
    margin-top: 0.2rem;
    flex-shrink: 0;
  }
  .reserve-lgpd a {
    color: inherit;
    text-decoration: underline;
  }
</style>
```

- [ ] **Passo 5: Verificar que o build segue passando**

```bash
pnpm ts:check && pnpm build
```

Esperado: 0 erros de tipo; `dist/index.html` existe; checkbox + label aparecem no source.

- [ ] **Passo 6: Commit**

```bash
git add src/components/ReserveForm.astro
git commit -m "feat(form): adiciona LGPD checkbox + data-whatsapp-number + script import"
```

---

## Tarefa 8: Criar `reserve-form.ts` (handler do DOM)

**Arquivos:**
- Criar: `src/scripts/reserve-form.ts`

**Por que sem unit test:** este arquivo é cola entre eventos do DOM e os utils puros (que estão 100% testados). O smoke test da Tarefa 10 é a verificação.

- [ ] **Passo 1: Criar o handler**

```ts
// src/scripts/reserve-form.ts
// Cola: valida form → dispara POST Formspree (fire-and-forget) → redireciona pra WhatsApp.
// Lógica pura mora em ./whatsapp.ts (testada via Vitest).
import { buildWhatsappUrl, getLeadFromFormData, maskPhone } from './whatsapp';

const FALLBACK_PHONE = '5535998224771';

function init() {
  const form = document.querySelector<HTMLFormElement>('[data-cinnamon-form]');
  if (!form) return;

  const phone = form.dataset.whatsappNumber ?? FALLBACK_PHONE;

  const tel = form.querySelector<HTMLInputElement>('input[name="telefone"]');
  if (tel) {
    tel.addEventListener('input', () => {
      tel.value = maskPhone(tel.value);
    });
  }

  form.addEventListener('submit', (e) => {
    e.preventDefault();

    if (!form.reportValidity()) return;

    const fd = new FormData(form);

    const gotcha = fd.get('_gotcha');
    if (typeof gotcha === 'string' && gotcha.length > 0) {
      window.location.href = '/privacidade';
      return;
    }

    const lead = getLeadFromFormData(fd);

    fetch(form.action, {
      method: 'POST',
      body: fd,
      headers: { Accept: 'application/json' },
    }).catch(() => {
      // Backup-only — WhatsApp é o canal real.
    });

    window.location.href = buildWhatsappUrl({ phone, lead });
  });
}

if (document.readyState !== 'loading') init();
else document.addEventListener('DOMContentLoaded', init);
```

- [ ] **Passo 2: Verificar que o TypeScript compila**

```bash
pnpm ts:check
```

Esperado: 0 erros.

- [ ] **Passo 3: Verificar que o script entra no bundle**

```bash
pnpm build
```

Esperado: build conclui; `dist/_astro/reserve-form.<hash>.js` (ou similar) existe. Confirmar com:

```bash
ls dist/_astro/ | grep -i reserve
```

Esperado: pelo menos um arquivo bate com o padrão.

- [ ] **Passo 4: Commit**

```bash
git add src/scripts/reserve-form.ts
git commit -m "feat(form): adiciona handler DOM (validate → POST backup → redirect WhatsApp)"
```

---

## Tarefa 9: Adicionar script Turnstile no `Base.astro`

**Arquivos:**
- Modificar: `src/layouts/Base.astro`

- [ ] **Passo 1: Adicionar a tag do script Turnstile**

Em `src/layouts/Base.astro`, localizar o bloco `<head>`. Após o `<meta property="og:image" ...>` existente (por volta da linha 40), adicionar — porém antes de `</head>`:

```astro
<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
```

Pra achar o local exato, procurar a tag `</head>` e inserir essa linha logo antes.

- [ ] **Passo 2: Verificar build**

```bash
pnpm build
```

Esperado: build conclui; `dist/index.html` contém `challenges.cloudflare.com/turnstile/v0/api.js`. Verificar com:

```bash
grep -l "turnstile/v0/api.js" dist/index.html
```

Esperado: retorna `dist/index.html`.

- [ ] **Passo 3: Commit**

```bash
git add src/layouts/Base.astro
git commit -m "feat(form): carrega script Turnstile no head do layout base"
```

---

## Tarefa 10: Setup externo (Formspree + Turnstile) e smoke test — HUMAN-IN-LOOP

**Arquivos:**
- Modificar: `src/components/ReserveForm.astro` (trocar 2 placeholders)

> **PARA — esses passos exigem contas que só o Luis pode criar. Pausar e pedir pro Luis fornecer as credenciais ou rodar a tarefa ele mesmo.**

- [ ] **Passo 1: Criar form no Formspree**

Manual:
1. Ir em https://formspree.io/, signup / login.
2. New form → nome "Cinnamon Drinks reservas" → email destino: `cinnamondrinks@gmail.com` (confirmar com Luis se for outro).
3. Copiar o **Form ID** (formato `mzbqwlry` ou `xanwjkpz`).

- [ ] **Passo 2: Criar site key do Turnstile no Cloudflare**

Manual:
1. Cloudflare dashboard → Turnstile → Add site.
2. Domínio: `cinnamondrinks.com.br` (produção) **e** `localhost` (dev/teste) — Turnstile aceita listar vários.
3. Modo do widget: **Managed**.
4. Copiar o **Site key** (formato `0x4AAAAAAA...`).

- [ ] **Passo 3: Trocar o placeholder de `action` em `ReserveForm.astro`**

Em `src/components/ReserveForm.astro`, substituir:

```
action="/.netlify/functions/-"
```

por (substituir `<FORM_ID>` pelo valor do Passo 1):

```
action="https://formspree.io/f/<FORM_ID>"
```

- [ ] **Passo 4: Trocar o placeholder do sitekey Turnstile**

No mesmo arquivo, substituir:

```
data-sitekey="TURNSTILE_SITEKEY_PLACEHOLDER"
```

por (substituir `<SITE_KEY>` pelo valor do Passo 2):

```
data-sitekey="<SITE_KEY>"
```

- [ ] **Passo 5: Smoke test em dev**

```bash
pnpm dev
```

Abrir http://localhost:4321, scrollar até o form de reserva e verificar:

1. **Widget Turnstile renderiza** (challenge aparece abaixo do checkbox LGPD).
2. **Máscara de telefone funciona** — digitar `35999998888` no campo WhatsApp; deve formatar pra `(35) 99999-8888`.
3. **Validação HTML5 dispara** — tentar submit vazio; tooltip nativo do browser aparece.
4. **Checkbox LGPD obrigatório** — preencher tudo menos o LGPD, dar submit; validação nativa bloqueia.
5. **Honeypot** — abrir DevTools, achar `<input name="_gotcha">`, setar valor via `document.querySelector('[name=_gotcha]').value = 'spam'`, submit válido. Deve redirecionar pra `/privacidade` e **não** abrir WhatsApp.
6. **Happy path** — preencher tudo com dados reais (email + WhatsApp do Luis). Submit. Verificar:
   - Aba Network: `POST https://formspree.io/f/<FORM_ID>` retorna 200 (ou 422 se a validação do Formspree pegar algo — logar o body e ajustar).
   - Email chega na inbox `cinnamondrinks@gmail.com` em ~30s.
   - Browser redireciona pra `https://wa.me/<num>?text=...` com a mensagem pré-preenchida contendo todos os campos enviados.

- [ ] **Passo 6: Commit das credenciais**

```bash
git add src/components/ReserveForm.astro
git commit -m "feat(form): conecta Formspree (form ID real) + Turnstile (site key real)"
```

> **NOTA:** Formspree Form ID e Turnstile Site Key são públicos por design (vivem no HTML do client). Ok commitar. **Não** commitar nenhuma API secret key do Formspree nem **secret key** do Turnstile — esses não aparecem no plano porque não precisamos de validação server-side (free tier dá conta).

---

## Self-review

**Cobertura do spec (vs `docs/patterns/forms.md` + Phase 8 do plano original):**
- ✅ Honeypot (`_gotcha`) — já existe no `ReserveForm.astro`, handler lê (Tarefa 8 Passo 1)
- ✅ Widget Turnstile — script no Base (Tarefa 9), widget no form (Tarefa 7), key real (Tarefa 10)
- ✅ Validação HTML5 nativa + `novalidate` + `reportValidity()` (Tarefa 8)
- ✅ Máscara de telefone (Tarefas 3 + 8)
- ✅ POST Formspree fire-and-forget (Tarefa 8)
- ✅ Redirect WhatsApp deeplink com todos os campos do lead (Tarefas 2 + 8)
- ✅ Checkbox LGPD obrigatório, desmarcado por padrão (Tarefas 5 + 7)
- ✅ Página de política linkada (Tarefa 6)
- ✅ `whatsappNumber` flui do `site.yml` via `data-whatsapp-number` (Tarefas 7 + 8)
- ✅ Testes para utils puros — 12 casos Vitest (Tarefas 2-4)
- ✅ Smoke test manual pro DOM glue (Tarefa 10)

**Type consistency:**
- Interface `Lead` definida uma vez em `whatsapp.ts` (Tarefa 2), importada pelo `whatsapp.test.ts` (Tarefas 2-4) e pelo `reserve-form.ts` (Tarefa 8 — implícito via tipo de retorno do `getLeadFromFormData`). ✅
- Assinatura `buildWhatsappUrl({ phone, lead })` consistente entre Tarefa 2 (definição), Tarefa 4 (`Lead` reutilizado), Tarefa 8 (call site). ✅
- `maskPhone(raw: string): string` — definido Tarefa 3, chamado Tarefa 8. ✅
- `getLeadFromFormData(fd: FormData): Lead` — definido Tarefa 4, chamado Tarefa 8. ✅

**Placeholder scan:** zero. Todo passo tem código concreto, paths exatos, comandos exatos.

**Isolamento de bloqueios externos:** Tarefas 1-9 podem ser feitas sem nenhuma conta externa. Só a Tarefa 10 precisa de credenciais Formspree + Turnstile, e está claramente marcada HUMAN-IN-LOOP no topo.
