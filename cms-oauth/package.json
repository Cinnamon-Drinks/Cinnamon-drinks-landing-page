{
  "name": "cinnamon-cms-oauth",
  "version": "0.0.0",
  "private": true,
  "description": "OAuth proxy do GitHub para o Sveltia CMS do Cinnamon Drinks",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types:check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.93.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd"]
  }
}
